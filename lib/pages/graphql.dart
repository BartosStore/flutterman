import 'package:flutter/material.dart';

class Graphql extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('GraphQl testing'),
      ),
      body: Text('TBD'),
    );
  }
}